package com.telerik.android.common;

/**
 * Represents the type of change that occurred in an {@link ObservableCollection}.
 */
public enum CollectionChangeAction {
    ADD,
    REMOVE,
    RESET,
    REPLACE,
    MOVE
}
